version: "3.4"

services:
{% for service in service_names.stdout_lines %}
  {{ service }}:
    deploy:
      placement:
        constraints:
          - node.role == worker
          - node.type == worker
{% endfor %}